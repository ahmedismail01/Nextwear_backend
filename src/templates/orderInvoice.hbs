<html>
  <head>
    <meta charset="UTF-8" />
    <title>Order Receipt</title>
  </head>
  <body
    style="font-family: Arial, sans-serif; margin:0; padding:20px; background:#f7f7f7;"
  >
    <div
      style="max-width:600px; margin:auto; background:white; padding:20px; border-radius:8px;"
    >

      <h2 style="color:#333;">Hello
        {{order.user.firstName}}
        {{order.user.lastName}},</h2>

      <p>Thank you for your purchase! Here is your receipt for order
        <strong>#{{order.trackingNumber}}</strong>
        placed on
        <strong>{{order.createdAt}}</strong>.</p>

      <hr />

      <h3>Order Summary</h3>

      <table
        width="100%"
        cellpadding="8"
        cellspacing="0"
        style="border-collapse: collapse;"
      >
        <thead>
          <tr style="background:#f0f0f0;">
            <th align="left">Item</th>
            <th align="center">Qty</th>
            <th align="right">Price</th>
          </tr>
        </thead>
        <tbody>
          {{#each order.products}}
            <tr>
              <td>{{this.name}} ({{this.size}} {{this.color}})</td>
              <td align="center">{{this.quantity}}</td>
              <td align="right">{{this.purchasePrice}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>

      <hr />

      <p><strong>Subtotal:</strong> {{order.finalPrice}}</p>
      <p><strong>Shipping:</strong> {{order.address.name}}</p>

      <h3>Total: {{order.finalPrice}}</h3>

      <hr />

      <h3>Shipping Information</h3>
      <p>
        {{order.address.name}}<br />
        {{order.address.city}},
        {{order.address.state}}
        {{order.address.zipCode}}<br />
        {{order.address.country}}
      </p>

      <p>If you have any questions, feel free to contact us.</p>

      <p style="margin-top:30px;">Best regards,<br />The NextWear Team</p>
    </div>
  </body>
</html>